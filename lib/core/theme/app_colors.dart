import 'package:flutter/material.dart';

/// Sport0Zero color palette
class AppColors {
  AppColors._();

  // Primary brand
  static const Color primary = Color(0xFF0D7C3E);      // Sport green
  static const Color primaryDark = Color(0xFF095C2D);
  static const Color primaryLight = Color(0xFF4CAF50);

  // Accent
  static const Color accent = Color(0xFFFF6B35);         // Energy orange
  static const Color accentDark = Color(0xFFE55A2B);

  // Neutrals
  static const Color background = Color(0xFFF8F9FA);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color surfaceDark = Color(0xFF1A1A2E);
  static const Color backgroundDark = Color(0xFF0F0F23);

  // Text
  static const Color textPrimary = Color(0xFF1A1A2E);
  static const Color textSecondary = Color(0xFF6B7280);
  static const Color textOnPrimary = Color(0xFFFFFFFF);
  static const Color textPrimaryDark = Color(0xFFF8F9FA);
  static const Color textSecondaryDark = Color(0xFF9CA3AF);

  // Status
  static const Color live = Color(0xFFEF4444);           // Red for live
  static const Color success = Color(0xFF10B981);
  static const Color warning = Color(0xFFF59E0B);
  static const Color error = Color(0xFFEF4444);

  // Dividers / borders
  static const Color divider = Color(0xFFE5E7EB);
  static const Color dividerDark = Color(0xFF374151);
}
